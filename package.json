{
    "scripts": {
        "generate-api-cli":"pnpm run generate-openapi && openapi-generator-cli generate -i openapi.json -g typescript-axios -o web/src/api",
        "generate-openapi":"TS_NODE_PROJECT=./api/tsconfig.json ts-node -r tsconfig-paths/register api/generate-openapi.ts"
    }
}