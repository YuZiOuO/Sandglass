{
  "name": "sandglass",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "lint:staged": "bun run --filter @sandglass/web lint:staged && bun run --filter @sandglass/api lint:staged",
    "check": "bun run --filter @sandglass/api check && bun run --filter @sandglass/web check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1"
  },
  "dependencies": {
    "hono": "^4.13.7"
  }
}
